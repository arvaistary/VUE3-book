# Часть 3. Границы ответственности: API, сервисы и middleware

## Учебная цель

После этой главы вы сможете отделить пользовательский интерфейс от сетевого
транспорта, вынести прикладную операцию в сервис и объяснить, почему
middleware маршрута не является местом для всех API-вызовов. Достаточно знать
JavaScript и базовый HTTP.

## Ситуация и риск для пользователя

Пользователь открывает карточку статьи. Один экран показывает понятную ошибку
при недоступной сети, другой падает с необработанным исключением, а третий
повторяет собственную сборку URL и отправляет неверный параметр. Для команды
это означает, что изменение серверного контракта нужно искать по компонентам,
а не менять в одном месте.

## Термины

**SSR (server-side rendering, серверный рендеринг)** — подготовка HTML на
сервере до загрузки клиентского JavaScript. В Nuxt часть сетевой логики может
выполниться в серверной, а затем в клиентской фазе.

**Слой ответственности** — участок системы с одной основной обязанностью.
Компонент отображает состояние и принимает действия. **Service layer (слой
сервисов)** выражает прикладную операцию, например «получить краткую статью».
**Transport adapter (транспортный адаптер)** знает, как выполнить HTTP-запрос.

**UI (user interface, пользовательский интерфейс)** — видимая часть сценария,
через которую человек получает результат и запускает действие.

**`$fetch`** — функция для выполнения сетевого запроса. В Nuxt её можно
использовать напрямую, но для общего транспорта и SSR нужно заранее выбрать
границу и способ передачи контекста.

**Nuxt plugin (плагин Nuxt)** — файл, который подключает общую зависимость при
запуске приложения. Суффиксы `.server` и `.client` ограничивают среду его
выполнения.

**`useNuxtApp`** — Nuxt-функция, через которую код получает зависимость,
предоставленную plugin-ом.

**`defineNuxtPlugin` и `defineNuxtRouteMiddleware`** — Nuxt-обёртки,
объявляющие соответственно plugin приложения и маршрутное middleware.

**Route middleware (маршрутное middleware)** выполняется вокруг навигации и
решает, можно ли продолжить переход. **Server middleware (серверное
middleware)** относится к серверной среде выполнения и обрабатывает входящий
запрос; это другая граница.

**Interceptor (перехватчик)** может централизованно обработать заголовки или
технические ошибки. Ни один из этих механизмов не должен незаметно становиться
вторым сервисным слоем.

## Минимальная задача

Оба варианта должны получить краткое описание статьи по идентификатору и
вернуть объект `{ title, excerpt }`. Входом служит строковый идентификатор и
функция транспорта. Проверка использует локальный транспорт, который не
подключается к сети, и отдельный вариант с ошибкой.

## Плохой пример

В обработчике кнопки удобно написать всё сразу: URL, запрос и преобразование
ответа находятся рядом с местом использования.

```js
async function onOpenArticle(articleId, show) {
  const response = await fetch(`https://api.example.invalid/articles/${articleId}`);
  const data = await response.json();
  show(data.title, data.excerpt);
}
```

Такой вариант правдоподобен для пробного экрана. Но UI знает адрес, формат
ответа и отсутствие ошибок. Идентификатор не кодируется, сетевой вызов нельзя
подменить в тесте, а одинаковые кнопки начнут расходиться. Если сервер вернёт
неуспешный ответ или изменит форму данных, пользователь получит необъяснимое
поведение.

## Хороший пример

Сервис говорит на языке прикладной операции, а транспорт передаётся ему как
зависимость.

```js
function createArticleService(request) {
  return {
    async getSummary(articleId) {
      const result = await request(`/articles/${encodeURIComponent(articleId)}`);

      if (!result.ok) {
        throw new Error('article request failed');
      }

      return {
        title: result.data.title,
        excerpt: result.data.excerpt,
      };
    },
  };
}

async function loadArticleForPage(service, articleId) {
  try {
    return { status: 'success', data: await service.getSummary(articleId) };
  } catch {
    return { status: 'error', message: 'Статью не удалось загрузить' };
  }
}

const localRequest = async (path) => ({
  ok: path === '/articles/intro',
  data: { title: 'Введение', excerpt: 'Короткое описание статьи' },
});

const service = createArticleService(localRequest);
```

Страница теперь получает `status` и решает, что показать. Сервис проверяет
контракт ответа, а транспорт можно заменить адаптером для SSR, браузера или
теста. Это добавляет небольшой слой и требует договориться о форме ошибки,
зато изменение HTTP-деталей не распространяется по UI.

Middleware здесь уместен для проверки маршрута: например, при отсутствии
необходимого признака навигация может быть остановлена или направлена на
страницу входа. Получение текста статьи остаётся ответственностью страницы и
сервиса. Такой порядок снижает вероятность циклических перенаправлений и
повторных запросов при каждой навигации.

## Как эти границы выглядят в Nuxt 3 и Nuxt 4

В старой структуре Nuxt 3 каталоги приложения обычно находятся в корне:

```text
plugins/
middleware/
server/middleware/
```

После перехода на стандартную структуру Nuxt 4 расположение частей приложения
становится явнее:

```text
app/plugins/
app/middleware/
server/middleware/
```

Первые два пути относятся к приложению: plugin подключает инфраструктуру, а
route middleware участвует в навигации. `server/middleware/` остаётся в
корневом `server/` и работает на серверной границе. Перемещение серверного
middleware в `app/middleware/` изменит его роль, а перемещение route middleware
в `server/middleware/` не даст ожидаемой проверки маршрута.

Плохой вариант может появиться, когда разработчик хочет «проверять запросы до
открытия страницы» и помещает сетевой вызов в маршрутное middleware:

```ts
export default defineNuxtRouteMiddleware(async () => {
  await $fetch('/api/article-list');
});
```

Теперь каждый переход потенциально запускает загрузку, даже если странице
нужны другие данные. При SSR и последующей клиентской навигации это может дать
лишний запрос и смешать транспорт с решением о маршруте.

Хороший вариант оставляет общую транспортную зависимость plugin-у, а решение
о навигации — route middleware:

```ts
export default defineNuxtPlugin(() => {
  const request = $fetch.create({ baseURL: '/api' });

  return { provide: { request } };
});
```

Страница или сервис обращается к этой зависимости явно:

```ts
const { $request } = useNuxtApp();
const articleService = createArticleService($request);
const article = await articleService.getSummary('intro');
```

Этот вызов показан на границе страницы или composable. Сам сервис получает
транспорт параметром и поэтому не обязан вызывать `useNuxtApp`: его можно
проверить тем же локальным адаптером, что и основной хороший пример. Сервис или
composable вызывает предоставленный клиент для своей операции, а
route middleware проверяет только маршрутный признак. В Nuxt 3 файл plugin
лежит в `plugins/`, в Nuxt 4 при новой структуре — в `app/plugins/`; суффикс
`.server` добавляют только после осознанной проверки среды выполнения.
Серверное middleware Nuxt/Nitro при этом остаётся отдельным механизмом.

Это не означает, что middleware никогда не должен получать данные. Проверка
сессии, нормализация адреса или защита маршрута могут требовать запроса. Но
такой запрос должен быть частью явно описанного маршрутного контракта, а не
скрытым способом загрузки данных каждой страницы.

## Сравнение и ограничения

Прямой `fetch` может быть хорошим выбором для маленького одноразового
примера. Когда операция повторяется, имеет разные реализации транспорта,
общий формат ошибки или серверный и клиентский контекст, выделение сервиса
окупается. Не стоит создавать сервис ради единственной константы и прятать в
нём разметку.

Плагин подходит для общей инфраструктуры: HTTP-клиента, логирования или
клиентского SDK. Прикладную операцию лучше оставить сервису. Interceptor
может добавить заголовок или нормализовать техническую ошибку, но не должен
решать, какую надпись показать пользователю.

## Упражнение

Добавьте в локальный транспорт ответ `{ ok: false }` и проверьте, что страница
получает `status: 'error'`, а не исключение наружу. Затем замените путь статьи
на идентификатор с пробелом и убедитесь, что сервис кодирует его. Наконец,
напишите псевдокод middleware для маршрута `/account`: перечислите условие,
действие при отказе и условие, которое исключает бесконечный redirect.

## Публичная проверка

`articles`, `api.example.invalid`, тексты и ответы полностью вымышлены.
Пример сервиса автономен: `createArticleService` использует локальный
`localRequest`, поэтому его можно проверить через `node --check` и
последовательные вызовы без внешнего API. Фрагмент Nuxt-кода проверяется
статически в учебном Nuxt-приложении. Термины сверены с [документацией Nuxt 3
о плагинах](https://nuxt.com/docs/3.x/directory-structure/plugins), [Nuxt 3
о middleware](https://nuxt.com/docs/3.x/directory-structure/middleware),
[Nuxt 4 plugins](https://nuxt.com/docs/4.x/directory-structure/app/plugins),
[Nuxt 4 middleware](https://nuxt.com/docs/4.x/directory-structure/app/middleware),
[руководством Nuxt 4 по обновлению](https://nuxt.com/docs/4.x/getting-started/upgrade).
