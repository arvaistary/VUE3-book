# Дополнительные gates — <название локального слоя>

Общие проверки находятся в [`workflow/core/`](../../core/README.md). Этот
шаблон нужен для правил, которые зависят от конкретного состава файлов,
команд или предметных рисков текущего репозитория.

Для каждого gate укажите:

- когда он применяется;
- какую ошибку блокирует;
- какая команда или конкретный тест его подтверждает;
- какой маркер записывается в DoD report.

## Контракт интерфейса

**Применяется при:** `<API, schema, CLI или другой публичный контракт>`.

- источник истины: `<путь или раздел>`;
- граница валидации: `<описание>`;
- ошибочные и граничные случаи: `<список>`;
- тесты: `<Class::test_method>`.

## Авторизация и приватность

**Применяется при:** `<новая роль, операция или доступ к данным>`.

Опишите `actor × operation × outcome`, порядок проверки прав и входных данных,
ожидаемые коды ошибок и правила, не допускающие раскрытия чужих идентификаторов.

## Состояния и мутации

**Применяется при:** `<изменение данных или переход состояния>`.

Зафиксируйте поведение для freeze, pending и удаления родительской сущности.

```markdown
| Операция | Freeze | Open pending | Deleted parent | Тест |
|----------|--------|--------------|----------------|------|
| <операция> | <результат> | <результат> | <результат> | <Class::test_method> |
```

## Побочные эффекты

**Применяется при:** `<event, listener, job, webhook, notification или audit>`.

Опишите границу транзакции, момент запуска, rollback, повторный запуск,
отсутствие дубликатов и тест реальной связки producer → handler → transport.

## Batch и concurrency

**Применяется при:** `<пакетная операция или race-sensitive изменение>`.

Опишите порядок блокировок, атомарность, runtime, число запусков и критерий
победы/проигрыша в гонке. Skipped-проверка не считается PASS.

## E2E и опасный ввод

Укажите сценарии, fixtures, динамические идентификаторы и проверку для
повреждённых данных, границ размера, кодировок, подменённого типа и переполнения
ресурсов.
