# Антипаттерны — <название локального слоя>

| Антипаттерн | Почему опасен | Как обнаружить | Чем заменить |
|-------------|---------------|----------------|--------------|
| `<пример>` | `<последствие>` | `<check, test или review>` | `<правильный паттерн>` |

Добавляйте только повторяемые ошибки, которые полезно ловить до финализации.
Разбор одной задачи должен оставаться в её review report.
