# Workflow-паттерны — <название проекта>

Используйте этот документ для правил машины состояний, авторизации, согласия,
freeze, границ транзакций, событий/jobs и concurrency, которые повторяются
между задачами.

## Pattern: <name>

- **Когда применяется:** `<условие>`
- **Разрешённый переход:** `<состояние и действие>`
- **Запрещённый переход:** `<ожидаемый код/404/rollback>`
- **Какие операции учесть:** `<строки mutation inventory>`
- **Проверка:** `<конкретный тест и runtime-доказательство>`

Не заменяйте этим документом contract конкретной задачи: её actor matrix,
mutation inventory и domain codes должны находиться в canonical spec.
