# Workflow-паттерны — <название локального слоя>

Используйте этот файл для повторяющихся правил состояний, прав, границ
транзакции, событий и конкурентного доступа.

## Паттерн: <название>

- **Когда применяется:** `<условие>`
- **Разрешённый переход:** `<состояние и действие>`
- **Запрещённый переход:** `<код, 404 или rollback>`
- **Какие операции учесть:** `<строки mutation inventory>`
- **Проверка:** `<конкретный тест и runtime-доказательство>`

Контракт отдельной задачи всегда остаётся в её каноническом `spec.md`.
