# Отчёт о технической целостности — <название локального слоя>

Зафиксируйте, какие обязанности выполняют общие проверки, а какие — локальные
команды этого репозитория. Укажите среду, команды и дату запуска.

| Слой | Ответственность |
|------|-----------------|
| `workflow/core/` | lifecycle, контракт, scope, provenance и evidence |
| `workflow/project/` | локальные команды, проверки и принципы |
| `technology-profile.env` | команды, runtime-метки и роли артефактов |
| `spec.md` | требования, состояния, права и обязательные доказательства |

## Проверенные обязанности

1. `<команды тестов и lint>`;
2. `<структурные и смысловые проверки>`;
3. `<E2E, security, side effects и concurrency>`;
4. `<пути артефактов и правила fixtures>`;
5. `<порядок запуска и ограничения>`.

## Ограничения

Перечислите режимы, которые не проверялись, и условия, при которых
`READY` нельзя объявлять.

## Evidence

Укажите commit, `base_ref`, команды verify/lint/finalize, runtime, результат
Evidence Index и дату. Общая фраза «проверено» не заменяет фактический вывод.
